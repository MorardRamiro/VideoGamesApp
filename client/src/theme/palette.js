
const white = '#FFFFFF';
const black = '#000000';
const grey = '#808080'
const green = '#2e7d32';
const blue = '#1976d2';
const lowTransparencyBlack = 'rgba(0, 0, 0, .9)';
const mediumTransparencyBlack = 'rgba(0, 0, 0, .8)';
const highTransparencyBlack = 'rgba(0, 0, 0, .25)';
const transparentYellow = 'rgba(250, 250, 0, .8)';

const theme = {
  white,
  black,
  grey,
  green,
  blue,
  lowTransparencyBlack,
  mediumTransparencyBlack,
  highTransparencyBlack,
  transparentYellow,
}

export default theme;
